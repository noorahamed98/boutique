{
  "name": "snehas-boutique-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack boutique app with React, Express, and PostgreSQL",
  "scripts": {
    "install:all": "npm install --include=dev --prefix server && npm install --include=dev --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "db:init": "npm run db:init --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
